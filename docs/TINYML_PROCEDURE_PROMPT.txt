====================================================================
STEP-BY-STEP PROCEDURE PROMPT — TINYML MODEL FOR EDGEWAKE
====================================================================

Copy-paste this prompt into ChatGPT, Claude, Gemini, or any AI assistant
when you're ready to start building. It gives the AI full context about
your project so it can guide you through each step interactively.

====================================================================
BEGIN PROMPT
====================================================================

I am building an Edge AI forest surveillance device called "EdgeWake" using an ESP32-CAM microcontroller. The device uses a tiered cascade architecture where it sleeps at ~10µA and only wakes up when hardware sensors (flame sensor on GPIO 13, vibration sensor SW-420 on GPIO 2) trigger an interrupt.

After waking, the device records 2 seconds of audio through an INMP441 I2S MEMS microphone (16 kHz, mono, 16-bit) and runs a TinyML audio classification model ON-DEVICE to verify if the threat is real before activating the camera and Wi-Fi.

I need to train a TinyML audio classification model with 3 classes:
1. "fire" — sounds of fire crackling, roaring, popping
2. "chainsaw" — motorized chainsaw/logging sounds
3. "background" — normal forest sounds (wind, birds, rain, silence, insects)

**My hardware constraints:**
- ESP32-CAM (AI-Thinker): 240 MHz Xtensa dual-core, 520 KB SRAM, 4 MB PSRAM, 4 MB Flash
- Model must be INT8 quantized (TFLite Micro)
- Inference must complete in < 500ms
- Model size must be < 500 KB (Flash) and < 100 KB (RAM during inference)
- I'm using Arduino IDE with the Edge Impulse Arduino library export

**My current firmware setup:**
- Audio sampling: 16 kHz, 2-second window, MFCC feature extraction
- The model integration code is already written in audio_utils.h (Edge Impulse `run_classifier()` API)
- config.h has a `USE_TINYML_MODEL` toggle (currently set to 0)
- Partition scheme: Huge APP (3MB No OTA / 1MB SPIFFS)

**Please guide me step-by-step through this COMPLETE procedure:**

1. **DATA COLLECTION** — Tell me exactly where to download training audio data (with direct links). I need the ESC-50 dataset classes for fire and chainsaw, and guidance on recording my own forest background sounds. Tell me how much data I need per class and in what format (sample rate, channels, bit depth).

2. **DATA PREPARATION** — Walk me through using Audacity to standardize all audio files: resampling to 16 kHz mono, normalizing volume, trimming silence, and exporting as WAV. Show me the exact Audacity steps.

3. **EDGE IMPULSE SETUP** — Guide me through creating a project on Edge Impulse Studio, uploading the audio data, labeling it with the 3 classes, and splitting into train/test sets.

4. **IMPULSE DESIGN** — Tell me the exact MFCC parameters to use (window size = 2000ms, coefficients = 13, frame length, stride, FFT length) and what neural network architecture to choose (Dense NN with 64→32→3 neurons, dropout 0.25).

5. **TRAINING** — Guide me through training the model with the right hyperparameters (epochs, learning rate, data augmentation settings). Tell me how to read the confusion matrix and what accuracy numbers are acceptable (target > 85%).

6. **TESTING** — Show me how to validate the model on the test set and interpret the results. Tell me red flags to watch for (class imbalance, overfitting, low per-class accuracy).

7. **EXPORT** — Walk me through exporting the trained model as an Arduino Library (.zip) with INT8 quantization selected. Show me how to install it in Arduino IDE.

8. **FIRMWARE INTEGRATION** — Tell me exactly which lines to change in my firmware:
   - config.h: Change USE_TINYML_MODEL from 0 to 1
   - audio_utils.h: Uncomment the Edge Impulse include and the inference code block
   - The function analyseAudioTinyML() should use run_classifier() and check if fire > 0.6 or chainsaw > 0.6

9. **COMPILATION & FLASH** — Guide me through compiling with the right board settings (AI Thinker ESP32-CAM, PSRAM enabled, Huge APP partition) and flashing via FTDI.

10. **FIELD TESTING** — Tell me how to test with the Serial Monitor, what the inference output should look like, and how to tune the confidence threshold if needed.

At each step, ask me if I've completed it before moving to the next. If I encounter errors, help me debug them. Give me exact commands, exact settings, and exact file paths — no vague instructions.

My operating system is Windows. My Arduino IDE version is 2.x. I have Node.js installed for the Edge Impulse CLI.

Start with Step 1 now.

====================================================================
END PROMPT
====================================================================


====================================================================
QUICK-START VARIANT (Shorter Prompt for Quick Sessions)
====================================================================

I'm building "EdgeWake" — an ESP32-CAM forest guardian device. I need to train a 3-class TinyML audio classifier (fire, chainsaw, background) using Edge Impulse and deploy it on ESP32. 

Specs: INMP441 mic at 16kHz, 2-second window, MFCC features, Dense NN (64→32→3), INT8 quantized, <200KB model. Arduino IDE, AI Thinker ESP32-CAM, 4MB PSRAM.

Walk me through: (1) getting training data from ESC-50, (2) Edge Impulse project setup, (3) MFCC + Dense NN impulse design, (4) training to >85% accuracy, (5) exporting as Arduino library, (6) integrating into my existing audio_utils.h firmware, (7) flashing and testing.

Be specific — give me exact settings, exact parameters, and exact code changes. Guide me one step at a time.

====================================================================
END QUICK-START PROMPT
====================================================================


====================================================================
DATASET DOWNLOAD PROMPT (For collecting training data specifically)
====================================================================

I need to collect audio training data for a 3-class TinyML environmental sound classifier. The classes are:

1. "fire" — crackling fire, roaring flames, fire popping
2. "chainsaw" — motorized chainsaw cutting, idling, revving  
3. "background" — forest ambience: wind, birds, rain, insects, silence, distant traffic

Requirements:
- Format: WAV, 16 kHz sample rate, 16-bit, Mono
- Minimum: 10 minutes per class (fire, chainsaw), 20 minutes for background
- Sources must be free/open-source licensed

Please:
1. Give me direct download links for the ESC-50 dataset and tell me which exact class numbers/folders to use for fire and chainsaw.
2. Recommend 3-5 specific Freesound.org URLs for high-quality forest background recordings.
3. Give me an Audacity workflow to batch-convert all files to 16kHz mono WAV.
4. Tell me how to organize the files into folders for Edge Impulse upload.
5. Warn me about common data quality issues (clipping, silence padding, class imbalance).

====================================================================
END DATASET PROMPT
====================================================================
